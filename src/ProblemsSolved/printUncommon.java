package ProblemsSolved;

public class printUncommon {
	
	/*
	 * Write a function that takes 2 strings as input and returns a string with uncommon characters of the input strings.
		Example: Input: String1 = "aabde" String2 = "cbaz"
	 */
	
	/*Pseudo Code
	 * 
	 * Convert the string1 to char[]
	 * And add the string2 to character List
	 * initialize output list
	 * Using for loop, If string1 characters are available in list, iterate to next element
	 * 	No, add into the list
	 */
	
	/*
	 * How to apply SET here
	 * 
	 * Remove the duplicates
	 * String inp1= "cbda";
        String inp2 ="aardy";
        System.out.println(inp1.replaceAll("["+inp2+"]+", ""));
     * And finally add the output in set 
	 */

}
